export default function routes() {
  this.resource('entities');
  this.resource('forms');
  this.resource('permissions');
  this.resource('jobs');
  this.resource('users');
  this.resource('files');
}
